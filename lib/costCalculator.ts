/**
 * Cost calculation utilities for lead cleaning jobs.
 * Estimates API costs based on lead count and processing options.
 */

export function estimateCost(leadCount: number): number {
  // Placeholder — implement pricing logic once API usage patterns are established
  return leadCount * 0.01
}
